{
  "name": "example-spa",
  "private": true,
  "type": "module",
  "dependencies": {
    "vue": "catalog:"
  },
  "devDependencies": {
    "@webwindowed/vite-plugin-cpp-header": "workspace:",
    "@webwindowed/web-api": "workspace:",
    "@tsconfig/node24": "catalog:",
    "@vitejs/plugin-vue": "catalog:",
    "@vue/tsconfig": "catalog:",
    "prettier": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vue-tsc": "catalog:"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build && vue-tsc --build",
    "preview": "vite preview",
    "format": "prettier --write .",
    "lint": "prettier --check ."
  }
}
